import test from 'node:test';import assert from 'node:assert/strict';import {Battle} from '../dist/simulation.js';
test('commanders start staggered and wait for the previous reply to clear',()=>{const b=new Battle(7);assert.equal(b.admirals[0].nextAt,30);assert.equal(b.admirals[1].nextAt,45);b.time=29.99;b.step(1/30,{});assert.equal(b.admirals[0].pending,true);assert.equal(b.admirals[1].pending,false);b.time=45;b.step(1/30,{});assert.equal(b.admirals[1].pending,false);b.setFallback(0);b.time=54;b.step(1/30,{});assert.equal(b.admirals[1].pending,false);b.time=55.1;b.step(1/30,{});assert.equal(b.admirals[1].pending,true);});
